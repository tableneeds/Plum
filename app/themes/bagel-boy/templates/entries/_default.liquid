<article class="bb-wrap bb-entry">
  <h1>{{ entry.title }}</h1>
  {% if entry.data.sections %}
    {{ entry.data.sections }}
  {% elsif entry.data.body %}
    <div class="prose">{{ entry.data.body | markdown }}</div>
  {% endif %}
</article>
